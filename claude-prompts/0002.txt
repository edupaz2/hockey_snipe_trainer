You are an expert Flutter developer specializing in cross-platform mobile apps with Bluetooth Low Energy (BLE) integration for Android and iOS.

You have created a complete, production-ready Flutter app that clones the core functionality of the SnipeLights app (https://snipelights.com). SnipeLights is an interactive hockey training system with 4 BLE-enabled light-up targets placed in a goal. The app controls the targets via BLE to run 13 game modes (e.g., "The Hockey Player’s Video Game" where targets light up randomly for 30 seconds; "Rapid Fire" for time-based hits; "Marksman" for repeated hits on one target; color-based games; reaction time challenges). Targets can be turned on/off, change colors (e.g., red, green, blue), and respond to hits.

Task: update the pubspec.yaml dependencies to new ones and adapt the code to the new dependencies updates.